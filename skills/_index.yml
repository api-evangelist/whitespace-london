generated: '2026-07-25'
method: generated
source: openapi/whitespace-london-platform-openapi.yml
note: >-
  Whitespace publishes no AGENTS.md, skill files or agent documentation. These skills are
  generated by API Evangelist from the published OpenAPI 3.0.0 document plus the public
  integration guides at apidocs.whitespace.co.uk. Every operation referenced is a verbatim
  method + path from the specification — the spec declares no operationIds, so operations are
  addressed by method and path (see overlays/whitespace-london-platform-overlay.yaml for the
  operationIds API Evangelist assigns). Nothing is invented. Note that most write operations on
  this platform are legally consequential market acts (offering a firm order, writing a line,
  signing a contract); the skills say so explicitly and there is no idempotency key to soften a
  mistaken retry.
skills:
- file: whitespace-london-broker-place-and-sign.md
  name: Place and sign a London-market risk as a broker
  role: broker
  api: openapi/whitespace-london-platform-openapi.yml
  operations:
  - POST /api/risks/newDraft
  - POST /api/risks/{riskID}/showToLeadUnderwriter
  - POST /api/risks/{riskID}/markAsFirmOrder
  - POST /api/risks/{riskID}/requestWrittenLineSets
  - POST /api/risks/{riskID}/signSets
- file: whitespace-london-underwriter-quote-and-write-line.md
  name: Quote a risk and write a line as an underwriter
  role: underwriter
  api: openapi/whitespace-london-platform-openapi.yml
  operations:
  - POST /api/risks/{riskID}/changeDraftNowQuoteInPreparation
  - POST /api/risks/{riskID}/saveQuoteDetails
  - POST /api/risks/{riskID}/showQuoteToBroker
  - POST /api/risks/{riskID}/quoteOnRequest
  - POST /api/risks/{riskID}/writeLine
- file: whitespace-london-defined-data-acord.md
  name: Extract and write contract data with the Defined Data API (ACORD tagset)
  role: either
  api: openapi/whitespace-london-platform-openapi.yml
  operations:
  - GET /api/v23.09/data/{riskID}
  - GET /api/v23.09/data/{riskID}/tagset/{tagset}
  - POST /api/v23.09/data/{riskID}/verify
  - POST /api/v23.09/data/{riskID}
  - GET /api/lookup/headings
- file: whitespace-london-queue-driven-integration.md
  name: Build a queue-driven Whitespace integration
  role: either
  api: openapi/whitespace-london-platform-openapi.yml
  events: asyncapi/whitespace-london-queues-asyncapi.yml
  operations:
  - GET /api/activities/{rootID}/full
  - POST /api/activities/filter
  - GET /api/risks/root/{rootID}
  - GET /api/documents/{docID}
  - POST /export/pdf/{riskID}
- file: whitespace-london-endorsement-lifecycle.md
  name: Run the endorsement lifecycle on a bound contract
  role: broker
  api: openapi/whitespace-london-platform-openapi.yml
  operations:
  - POST /api/risks/{riskID}/createEndorsement
  - POST /api/risks/{EndorsementID}/showEndorsement
  - POST /api/risks/{EndorsementID}/acceptEndorsement
  - POST /api/risks/{riskID}/completeEndorsement
  - POST /api/bindNotifyParties
